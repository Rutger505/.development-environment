#!/bin/bash
set -euo pipefail

branch="${1:?Usage: dev-env-create-worktree <branch>}"

repo="$(basename "$(pwd)")"
worktree_path="../${repo}-worktrees/${branch}"

git worktree add -b "$branch" "$worktree_path"

echo "cd to $worktree_path"
cd "$worktree_path"
exec "$SHELL"
